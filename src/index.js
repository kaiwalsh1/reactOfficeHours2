// Import from react allows us to use JSX syntax
import React from 'react';
// React DOM takes our app and renders to the page
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);
